import { ChatNavbar, ChatSearch } from "..";

const ChatSidebar = () => {
  return (
    <div className="chatSidebar">
      <ChatNavbar />
      <ChatSearch />
    </div>
  );
};

export default ChatSidebar;
